import styled from "styled-components";

export const SignInInputBoxesWrapper = styled.div<any>(() => ({
  borderRadius: 3,
}));

export const SignInDiv = styled.div<any>(() => ({
  width: 250,
  border: "1px solid #e7e7e7",
}));
